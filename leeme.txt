#Luego de clonar el repo: ejecuté estos comandos:
sudo composer init

#Luego quise consultar el estado del repo y me arrojó un error:
git config --global --add safe.directory /Applications/XAMPP/xamppfiles/htdocs/morelliadminprop

#Luego quise trabajar en el proyeto, me arrojaba [permisos insuficientes] Para dar permisos al VS:
sudo chown -R leorodriguez /Applications/XAMPP/xamppfiles/htdocs/

#BBDD

ALTER TABLE `EXP_BUILDING` ADD `fk_sp_provincias` INT NOT NULL AFTER `cp`, ADD `fk_sp_partidos` INT NOT NULL AFTER `fk_sp_provincias`, ADD `fk_sp_localidades` INT NOT NULL AFTER `fk_sp_partidos`;
ALTER TABLE `EXP_BUILDING` ADD `cp` INT NOT NULL AFTER `number`;
